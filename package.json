{
  "name": "login-demo-qtm",
  "version": "1.0.0",
  "description": "Demo login website with vulnerabilities",
  "main": "server.js",
  "scripts": {
    "start:dev": "dotenv -e .env node login/dev/server.js",
    "start:stable": "dotenv -e .env node login/stable/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:up": "docker-compose --env-file .env up",
    "docker:build": "docker-compose --env-file .env up --build",
    "docker:down": "docker-compose down"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "dotenv-cli": "^7.4.2",
    "express": "^4.19.2",
    "pg": "^8.11.5"
  },
  "author": "",
  "license": "ISC"
}
